# TaoxinAI

`TaoxinAI` 是讨薪方向的独立重构目录。

负责人：我

这个目录不替换当前旧后端，而是作为新的分阶段法律论证系统骨架使用。这里的目标是：

- 保留原项目可运行原型作为参考
- 在新目录下搭建更清晰的后端结构
- 为后续的争点识别、事实抽取、要件匹配、IRAC 生成和验证预留稳定接口

当前阶段这里已经包含：

- 新的 FastAPI 入口
- 结构化接口骨架
- 分阶段 pipeline 占位实现
- 检索、分析、模板、测试目录骨架
